{
  "version": 2,
  "name": "musicServer",
  "builds": [
      { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
      {
        "src": "/api/(.*)",  // Match only API routes
        "dest": "index.js"
         
      }
  ]
}
